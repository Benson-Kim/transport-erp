# Hooks Directory

Custom React hooks for the application.

## Guidelines

- Prefix with "use" (useAuth, useForm, etc.)
- Document with JSDoc
- Handle cleanup in useEffect
- Export types for hook returns
